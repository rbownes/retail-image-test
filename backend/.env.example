# Required for Claude (placement + critique agents).
ANTHROPIC_API_KEY=

# Required only when provider=openai.
OPENAI_API_KEY=

# Optional overrides.
# OUT_DIR=./out
# ASSETS_DIR=./assets
# JOBS_DIR=./out/jobs
# MAX_COST_USD_PER_JOB=2.00
# CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# PRODUCT_NAME=CanvasKit
# PRODUCT_TAGLINE=Brief to brand-ready creative in under a minute.
